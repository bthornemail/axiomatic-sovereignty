# SKILLS.md — Axiomatic Sovereignty Agent Skills Contract

```text
CONFIGURATION: SIX-PART RELATIONAL AUTHORITY GEOMETRY
CONFIGURATION: FIVE-PART REUSABLE FOUNDATION SUBSTRATE
CONFIGURATION: 17-TIER MASTER COSMOLOGICAL MATRIX
CONFIGURATION: CONCENTRIC OSI STRATUM MAPPING
CONFIGURATION: FIVE-FILE PERMUTATION LOCK
CONFIGURATION: FIVE-LAYER LIFT MAPPING
CONFIGURATION: TRI-LAYER BLACKBOARD RESOLUTION
CONFIGURATION: MACHINE-READABLE SUBSTRATE INTEGRITY
CONFIGURATION: COQ AXIOMATIC PROMOTION LOCK
CONFIGURATION: OMINO CENTROID PHASE POSITION LOCK
NOMENCLATURE STATUS: FROZEN PHASE LOCK
CORE: ALGORITHMIC DETERMINISM ONLY
FORM: OUTSIDE-IN CARRIERS, SCAFFOLDS, PROOFS, BACKENDS, VIEWS
```

Read each skill block as an immutable recipe over fixed-width coordinates. Do
not turn these blocks into an object model, evaluator, AST pipeline, delayed CPU
state, name binder, policy layer, or sequential worker grid.

---

## SKILL_01: ENFORCE_SIX_PART_RELATIONAL_AUTHORITY

```text
PURPOSE: Declare the six-part relational authority geometry governing the
         axiomatic-sovereignty repository structure.

AUTHORITY:
  rules        -> node        -> hypotenuse -> 0-sphere
  facts        -> edge        -> circle     -> sphere
  closures     -> graph       -> triangle   -> tetrahedron
  combinators  -> incidence   -> square     -> cube
  constructs   -> multigraph  -> pentagon   -> tesseract
  configurations -> hypergraph -> hexagon   -> composed topology

TYPE: Structural geometry enforcement
INPUT: 6 folder authorities and their geometric mappings
OUTPUT: Canonical six-part sequence preserved across all surfaces
CONSTRAINTS:
  - The six forms are not navigation documents; they define public
    relational authority
  - Canonical sequence is: rules -> facts -> closures -> combinators
    -> constructs -> configurations
  - The geometric sequence is: hypotenuse -> circle -> triangle ->
    square -> pentagon -> hexagon
PROTOCOL:
  1. Verify the six-part folder structure exists at the repository root
  2. Verify each authority folder carries its geometric name
  3. Verify the canonical sequence is preserved in all documentation
ANTI_PATTERN: Treating the six forms as who/what/when/where/why/how
              narrative categories instead of relational authority
```

---

## SKILL_02: ENFORCE_FIVE_PART_REUSABLE_FOUNDATION

```text
PURPOSE: Maintain the five-part reusable geometric substrate carried by
         the first five relational authorities.

FOUNDATION:
  rules        -> node       -> 0-sphere
  facts        -> edge       -> sphere
  closures     -> graph      -> tetrahedron
  combinators  -> incidence  -> cube
  constructs   -> multigraph -> tesseract

TYPE: Structural geometry enforcement
INPUT: 5 folder authorities and their spatial geometry mappings
OUTPUT: Canonical five-part foundation sequence preserved
CONSTRAINTS:
  - The five reusable files form the persistent foundation
  - rules = node authority, invariant/obligation surface, 0-sphere origin
  - facts = edge authority, observable relation surface, spherical extension
  - closures = graph authority, bounded completion surface, tetrahedral closure
  - combinators = incidence authority, lawful composition surface, cubic coordination
  - constructs = multigraph authority, simultaneous relational memory, tesseract coordination
PROTOCOL:
  1. Verify the five-part foundation exists
  2. Verify the spatial geometry sequence is preserved
  3. Verify the foundation is not described as separate from the six-part authority
ANTI_PATTERN: Describing the five-part foundation and six-part authority as
              unrelated frames
```

---

## SKILL_03: ENFORCE_SIX_EXTENDS_FIVE

```text
PURPOSE: Ensure the six-part authority correctly extends the five-part
         foundation by adding configurations.

RELATIONSHIP:
  FIVE-PART FOUNDATION: rules, facts, closures, combinators, constructs
  SIX-PART AUTHORITY:   rules, facts, closures, combinators, constructs, configurations

  configurations extends the five-part reusable foundation
  configurations does not replace it
  configurations assembles those reusable substrates into a public
  hypergraph configuration

TYPE: Structural relationship enforcement
INPUT: The six-part and five-part structures
OUTPUT: The extension relationship is correctly described
CONSTRAINTS:
  - configurations is the sixth authority, not a replacement for any
    of the first five
  - The five-part foundation is reusable; configurations composes it
PROTOCOL:
  1. Verify configurations/ exists as the sixth authority
  2. Verify the five-part foundation is described as reusable substrate
  3. Verify the extension relationship is documented
ANTI_PATTERN: Describing configurations as part of the foundation rather
              than an extension of it
```

---

## SKILL_04: ENFORCE_17_TIER_MASTER_MATRIX

```text
PURPOSE: Maintain the 17-Tier Master Cosmological Matrix from -8 to +8.

MATRIX:
  +8 CONTRACTS      Behavioral specification agreements / AAL invariants
  +7 ALGORITHMS     Branchless O(1) state transformation procedures
  +6 PROOFS         64-module active admit-free Coq proof book
  +5 AXIOMS         First-principles foundational assertions
  +4 TYPES          Fixed-width classification and custody bounds
  +3 SEMANTICS      Meaning assignments mapped to designated domains
  +2 DOMAINS        Bounded universes and n=6 truth-table surfaces
  +1 SYNTAX         Rules for well-formed bit formulas
   0 SYMBOLS        OMINO raw tokens / OMINO Centroid at 0x00 / 0 degrees
  -1 RULES          Point / Node / Hypotenuse / 0-Sphere
  -2 FACTS          Line / Edge / Circle / Sphere
  -3 CLOSURES       Triangle / Graph / Plane / Tetrahedron
  -4 COMBINATORS    Square / Incidence / Volume / Cube
  -5 CONS           Pentagon / Multigraph / Solid / Tesseract
  -6 CONFIGURATIONS Hexagon / Hypergraph / Topology / Hyper-Volume
  -7 CLAUSES        Fano Plane / [7,4,3] SEC / Meta-Cons / .omi-.imo carriers
  -8 MODULES        Octagon / Octonion / COBS-CONS / hidden .o executables

TYPE: Matrix enforcement
INPUT: The 17-tier master matrix and its hidden targets
OUTPUT: Front-matter synchronized with hidden targets
CONSTRAINTS:
  - The Matrix is a routing contract, not permission to leak dependencies
  - Visible Markdown may carry YAML front matter linking to hidden targets
  - No ad hoc compiler loops, text-parser pipelines, hidden mutable state,
    or cross-repository dependency grafts
  - Hidden .o/.omi/.imo tree is under ./.omi/
PROTOCOL:
  1. Verify the 17-tier matrix is documented
  2. Verify hidden targets exist under ./.omi/
  3. Verify front-matter links are consistent with hidden targets
  4. Verify no cross-repository dependency leakage
ANTI_PATTERN: Using the Matrix as permission to create ad hoc compiler loops
              or text-parser pipelines
```

---

## SKILL_05: ENFORCE_CONCENTRIC_OSI_STRATUM

```text
PURPOSE: Enforce the concentric OSI stratum mapping across the
         multi-project ecosystem.

STRATUM:
  LAYER  7  APPLICATION       metaverse-kit / metaverse-build
  LAYER  6  PRESENTATION      mind-git / mind-git-vr-ecosystem
  LAYER  5  SESSION           axiomatic-sovereignty
  LAYER  4  TRANSPORT         setco-framework-model
  LAYER  3  NETWORK           omnicron-epistemic-model
  LAYER  2  DATA LINK         omi-object-model
  LAYER  1  PHYSICAL          emergent-axial-lisp
  LAYER  0  SUBSTRATE CORE    omino-tensor-model
  LAYER -1  COGNITIVE ORIGIN  god-is-word

TYPE: Architecture enforcement
INPUT: Repository boundaries and stratum assignments
OUTPUT: Each layer remains autonomous
CONSTRAINTS:
  - Each layer is autonomous
  - Communication between layers approaches from the outside in
  - .omi, .imo, .o surfaces are the declared transitions
  - No dependency leakage across repository boundaries
  - No intermixing of data models, build dependencies, runtime state,
    or implementation abstractions across boundaries
PROTOCOL:
  1. Verify no imports from other layers exist in source
  2. Verify .omi/.imo/.o surfaces are used for layer transitions
  3. Verify no cross-layer dependency leakage
ANTI_PATTERN: Importing or vendoring code from another layer to make a
              local task pass
```

---

## SKILL_06: ENFORCE_FIVE_FILE_PERMUTATION_LOCK

```text
PURPOSE: Maintain the five-file permutation lock for the C-substrate.

PERMUTATIONS:
  (0,1,2,3,4)  identity
  (0,1,2,4,3)  swap COMBINATORS and CONS
  (0,1,4,3,2)  rotate CLOSURES to CONS
  (0,4,2,3,1)  rotate FACTS to CONS
  (4,1,2,3,0)  rotate RULES to CONS

TYPE: Substrate enforcement
INPUT: The five-file C-substrate
OUTPUT: Exactly five files with distinct named authorities
CONSTRAINTS:
  - Exactly five files in the substrate
  - Five distinct named authorities
  - No generic naming allowed
  - The centroid is always CONS
PROTOCOL:
  1. Verify exactly five .o files exist
  2. Verify each file carries a distinct named authority
  3. Verify CONS is always the centroid
ANTI_PATTERN: Using generic names or adding a sixth file to the substrate
```

---

## SKILL_07: ENFORCE_FIVE_LAYER_LIFT_MAPPING

```text
PURPOSE: Maintain the five-layer lift mapping for fold operations.

LIFT:
  rules      <- 0  (vertex 0, hypotenuse origin)
  facts      <- 1  (vertex 1, sphere extension)
  closures   <- 2  (vertex 2, tetrahedron closure)
  combinators <- 3 (vertex 3, cube coordination)
  cons       <- 4  (centroid, tesseract coordination)

TYPE: Fold enforcement
INPUT: Fold operations and their vertex mappings
OUTPUT: Every fold lifts to the correct vertex
CONSTRAINTS:
  - Fold always lifts to the correct vertex
  - No generic recursion replaces fold-lift
  - The lift is branchless relative to the fixed OMINO Centroid
PROTOCOL:
  1. Verify fold operations reference the correct vertex
  2. Verify no generic recursion is used
  3. Verify the lift is branchless
ANTI_PATTERN: Using generic recursion instead of fold-lift
```

---

## SKILL_08: ENFORCE_TRI_LAYER_BLACKBOARD_RESOLUTION

```text
PURPOSE: Enforce the Tri-Layer Blackboard Resolution pattern.

LAYERS:
  *.omi    User-Local Resolution    low plane ID boundary mapping
           nibble-interleaved form 0xY0X0
  *.o      Local-Binary Resolution  real-time algorithmic register resolution
  *.imo    User-Remote Resolution   high-plane data-omni boundary
           0xFFFFFFFF 0xFFFFFFFF

TYPE: Resolution enforcement
INPUT: The tri-layer blackboard surfaces
OUTPUT: Three distinct resolution layers preserved
CONSTRAINTS:
  - Do not replace with linear parser staging, mutable heap records,
    or sequential memory access
  - The three layers remain distinct and non-leaking
PROTOCOL:
  1. Verify .omi, .o, .imo surfaces exist and are distinct
  2. Verify no linear parser staging is used
  3. Verify no mutable heap records are used
ANTI_PATTERN: Replacing the tri-layer pattern with linear parser staging
```

---

## SKILL_09: ENFORCE_MACHINE_READABLE_SUBSTRATE

```text
PURPOSE: Ensure all machine-readable files reside inside the hidden .omi/
         directory at the repository root.

SUBSTRATE:
  .omi/
    rules.{omi,imo,o}
    facts.{omi,imo,o}
    closures.{omi,imo,o}
    combinators.{omi,imo,o}
    constructs.{omi,imo,o}
    configurations.{omi,imo,o}
    clauses.{omi,imo}
    modules.o

TYPE: File organization enforcement
INPUT: The .omi/ directory and its contents
OUTPUT: All 21 machine files reside flat in .omi/
CONSTRAINTS:
  - No nesting inside .omi/
  - All 21 files go flat in a single hidden folder
  - The visible workspace is reserved for human-readable Markdown
  - The .gitignore must allow .omi/*.o files
PROTOCOL:
  1. Verify .omi/ exists at the repository root
  2. Verify all 21 files exist flat inside .omi/
  3. Verify no files are nested in subdirectories
  4. Verify .gitignore allows .omi/*.o files
ANTI_PATTERN: Nesting files inside subdirectories within .omi/
```

---

## SKILL_10: ENFORCE_COQ_AXIOMATIC_PROMOTION_LOCK

```text
PURPOSE: Protect the active Coq proof registry from forbidden constructs.

ACTIVE REGISTRY: /home/main/omi/omi-axioms/coq/00..04

TARGET: 64 active modules:
  23 existing active modules
  + 38 independently promoted archive basenames
  + 3 active bridge modules
  = 64 active modules

FORBIDDEN IN ACTIVE SOURCES:
  Admitted, admit, Axiom, Parameter, Conjecture, Abort

PRESERVED NAMES:
  OMINO, computed_omino, overall_mismatch,
  make proof-registry-lock, make proof-strict

TYPE: Proof authority enforcement
INPUT: Active Coq sources and their proof registry
OUTPUT: 64 admission-free active modules
CONSTRAINTS:
  - No forbidden constructs in active sources
  - No deprecated OMINO-name variants restored
  - No archived drafts cited as active proof authority unless the
    active counterpart proves that exact statement
  - Every repaired archive basename enters as its own canonical file
  - No merging, collapsing, or combining promoted basenames
PROTOCOL:
  1. Verify no forbidden constructs exist in active sources
  2. Verify the active registry has 64 modules
  3. Verify no deprecated names are used
  4. Verify no archived drafts are cited as active authority
ANTI_PATTERN: Using Admitted or Axiom to bypass construction
```

---

## SKILL_11: ENFORCE_OMINO_CENTROID_ORIGIN

```text
PURPOSE: Ensure every coordinate derivation references the fixed OMINO
         Centroid at 0x00 and 0 degrees.

CENTROID:
  OMINO Centroid
  pinch point: 0x00
  azimuth: 0 degrees

TYPE: Coordinate origin enforcement
INPUT: Arithmetic kernels and coordinate derivations
OUTPUT: All derivations reference the fixed OMINO Centroid
CONSTRAINTS:
  - OminoCentroid / OMINO_CENTROID_POINT is the projective origin
  - All rotations are relative to the fixed centroid
  - Antipodal orientation is relative to the same centroid
  - No second origin is introduced
PROTOCOL:
  1. Verify all coordinate derivations reference the OMINO Centroid
  2. Verify no second origin is introduced
  3. Verify antipodal orientation is relative to the same centroid
ANTI_PATTERN: Introducing a second origin or using absolute coordinates
```

---

## SKILL_12: ENFORCE_GAUGE_INCIDENCE_WITNESS

```text
PURPOSE: Verify the 0x18 gauge-incidence witness across all four sectors.

WITNESS:
  0x00 ^ 0x1B ^ 0x1C ^ 0x1F == 0x18
  0x20 ^ 0x3B ^ 0x3C ^ 0x3F == 0x18
  0x40 ^ 0x5B ^ 0x5C ^ 0x5F == 0x18
  0x60 ^ 0x7B ^ 0x7C ^ 0x7F == 0x18

INVARIANT: 0x18 == 24 decimal == 4!

TYPE: Witness enforcement
INPUT: Omicron gauge boundaries and Metatron Incidence Scribe surfaces
OUTPUT: Every sector reduces to 0x18
CONSTRAINTS:
  - High sector prefix cancels under XOR
  - Metatron is not called a parser or generic resolver
  - The 0x18 witness belongs to Metatron Incidence Scribe
PROTOCOL:
  1. Verify all four sectors reduce to 0x18
  2. Verify the high sector prefix cancels under XOR
  3. Verify Metatron is correctly attributed
ANTI_PATTERN: Calling Metatron a parser, policy layer, or generic resolver
```

---

## SKILL_13: ENFORCE_SECDDED_STREAMING_CELL

```text
PURPOSE: Verify the canonical [8,4,4] SECDED streaming cell structure.

BIT MAP:
  Bit 0 -> LOGOS  -> parity check position P1
  Bit 1 -> NOMOS  -> parity check position P2
  Bit 2 -> FS     -> data position D1 -> FACTS
  Bit 3 -> PATHOS -> parity check position P4
  Bit 4 -> GS     -> data position D2 -> RULES
  Bit 5 -> RS     -> data position D3 -> COMBINATORS
  Bit 6 -> US     -> data position D4 -> CLOSURES
  Bit 7 -> OMINO  -> overall parity hinge -> CONS

OUTCOME MATRIX:
  syndrome == 0, overall_mismatch == 0 -> no detected error
  syndrome != 0, overall_mismatch == 1 -> correct interior bit 0..6
  syndrome == 0, overall_mismatch == 1 -> correct OMINO bit 7
  syndrome != 0, overall_mismatch == 0 -> double error detected

TYPE: SECDED enforcement
INPUT: The extended Hamming bit-vector map
OUTPUT: The SECDED outcome matrix holds
CONSTRAINTS:
  - overall_mismatch == LOGOS ^ NOMOS ^ FS ^ PATHOS ^ GS ^ RS ^ US ^ OMINO
  - The bit-7 hinge and OMINO centroid are one canonical OMINO, not two names
  - Do not name this value parity_valid in arithmetic kernels
PROTOCOL:
  1. Verify the bit map is correct
  2. Verify the outcome matrix is preserved
  3. Verify the bit-7 hinge and OMINO centroid are one canonical OMINO
ANTI_PATTERN: Naming the overall_mismatch value parity_valid
```

---

## SKILL_14: ENFORCE_FIVE_FILE_SUBSTRATE

```text
PURPOSE: Maintain the five-file C-substrate as the machine-readable binary layer.

SUBSTRATE: rules.o, facts.o, closures.o, combinators.o, cons.o

TYPE: Substrate enforcement
INPUT: The five .o files
OUTPUT: Exactly five .o files compile from .omi/.imo surfaces
CONSTRAINTS:
  - Each carries [4320 | 4320^2 | 4320^4] incidence surfaces
  - The .o layer is the direct local register substrate
  - .omi/.imo surfaces remain declaration and definition views
PROTOCOL:
  1. Verify exactly five .o files exist
  2. Verify each carries the three incidence surfaces
  3. Verify the .o layer is the direct local register substrate
ANTI_PATTERN: Adding a sixth .o file or treating .o as application binaries
```

---

## SKILL_15: MAINTAIN_CIRCULAR_SLIDE_RULER

```text
PURPOSE: Maintain the hosted structure of one omino tile.

STRUCTURE:
  One omino Tile
    16 Blackboard Face Planes
    18 Local240 Blackboard Faces
      4 Blackboard Quadrants per face
        1 Circular Slide Ruler per quadrant
          60 Circular Ruler Positions

THEREFORE:
  18 * 4 = 72 Circular Slide Rulers per omino tile
  72 * 60 = 4,320 local incidences per omino tile

TYPE: Structure enforcement
INPUT: The omino tile structure
OUTPUT: The 4,320 local incidences are preserved as local coordinate surface
CONSTRAINTS:
  - The 4,320 local incidences are not distributed across memory or arrays
  - They are the local coordinate surface of one unified omino tile context
  - The 72 rulers are not iterated as sequential row tests
PROTOCOL:
  1. Verify the omino tile structure is correct
  2. Verify the 4,320 incidences are not distributed across memory
  3. Verify the 72 rulers are not iterated as sequential row tests
ANTI_PATTERN: Distributing the 4,320 incidences across program memory
```

---

## SKILL_16: ENFORCE_FOLD_LIFTING

```text
PURPOSE: Ensure all fold operations use the five-file lift mapping.

LIFT:
  rules      <- 0  (vertex 0, hypotenuse origin)
  facts      <- 1  (vertex 1, sphere extension)
  closures   <- 2  (vertex 2, tetrahedron closure)
  combinators <- 3 (vertex 3, cube coordination)
  cons       <- 4  (centroid, tesseract coordination)

TYPE: Fold enforcement
INPUT: Fold operations and their vertex mappings
OUTPUT: Every fold is a deterministic lift to the correct vertex
CONSTRAINTS:
  - Fold always lifts to the correct vertex
  - No generic recursion replaces fold-lift
  - The lift is branchless relative to the fixed OMINO Centroid
PROTOCOL:
  1. Verify fold operations reference the correct vertex
  2. Verify no generic recursion is used
  3. Verify the lift is branchless
ANTI_PATTERN: Using generic recursion instead of fold-lift
```

---

## SKILL_17: MAINTAIN_COBS_CONS_BLACKBOARD

```text
PURPOSE: Maintain the COBS CONS 16x16 Blackboard Pattern.

BOARD: The full 256-byte space as one 16x16 byte board
  row = high nibble, col = low nibble

SURFACES:
  Local Board Surface   0x00..0x7F   User-Local   CAR / *.omi
  Remote Board Surface  0x80..0xFF   User-Remote  CDR / *.imo

BOUNDING RECTANGLES:
  Local:
    (0x00, 0x07, 0x37, 0x30)
    (0x08, 0x0F, 0x3F, 0x38)
    (0x40, 0x47, 0x77, 0x70)
    (0x48, 0x4F, 0x7F, 0x78)
  Remote:
    (0x80, 0x87, 0xB7, 0xB0)
    (0x88, 0x8F, 0xBF, 0xB8)
    (0xC0, 0xC7, 0xF7, 0xF0)
    (0xC8, 0xCF, 0xFF, 0xF8)

TARGET REDUCTION: CONS = 0xFFFFFFFF

TYPE: Blackboard enforcement
INPUT: The COBS CONS 16x16 Blackboard Pattern
OUTPUT: The Blackboard Pattern is preserved
CONSTRAINTS:
  - No row or column traversal as nested loops inside the core
  - The target reduction is 0xFFFFFFFF
PROTOCOL:
  1. Verify the 16x16 board structure is correct
  2. Verify the bounding rectangles are correct
  3. Verify no nested loops are used for traversal
ANTI_PATTERN: Using nested loops for row or column traversal
```

---

## SKILL_18: ENFORCE_PASCAL_LAYER_MULTIPLICITY

```text
PURPOSE: Enforce the hardwired Layer-4 Pascal multiplicity map.

MAP:
  0x0             -> 0
  0x1,0x7,0xF     -> 1
  0x2,0x3,0x5,0x6 -> 4
  0x8,0x9,0xC,0xD -> 4
  0x4,0xA,0xE     -> 6
  0xB             -> 12

TYPE: Multiplicity enforcement
INPUT: The Pascal multiplicity map
OUTPUT: The map is implemented as a fixed 16-entry table
CONSTRAINTS:
  - Implement as a fixed 16-entry table or equivalent bit-mask
  - Do not infer weights from runtime state
PROTOCOL:
  1. Verify the map is implemented as a fixed 16-entry table
  2. Verify no runtime state inference is used
ANTI_PATTERN: Inferring weights from runtime state
```

---

## SKILL_19: VALIDATE_LOCAL_INCIDENCE_SURFACES

```text
PURPOSE: Verify the three-tier OMINO incidence geometry.

TIERS:
  4320       base string surface
  4320^2     interaction surface    = 18,662,400 relational points
  4320^4     meta-matrix horizon    = 348,285,173,760,000 coordinate slots

FIELD: Klein (60,15) resolution form: QK(x,y) = 60x^2 + 16xy + 4y^2
  At x = 15, cross term 16*15*y = 240y vanishes mod 240.

TYPE: Incidence enforcement
INPUT: The three-tier incidence geometry
OUTPUT: Each A-list face carries the three-tier stack
CONSTRAINTS:
  - The surfaces are coordinate counts, not sequential row tests
  - The field is the Klein (60,15) form
  - Cross term vanishes under modulo-240 Algorithmic Clock
PROTOCOL:
  1. Verify each A-list face carries the three-tier stack
  2. Verify the field is the Klein (60,15) form
  3. Verify the cross term vanishes mod 240
ANTI_PATTERN: Implementing the surfaces as sequential row tests
```

---

## SKILL_20: ENFORCE_HASHLESS_REMOTE_RESOLUTION

```text
PURPOSE: Ensure remote resolution depends only on coordinate coherence.

SHARED STATE:
  [LOGOS NOMOS FS PATHOS GS RS US OMINO]
  OMINO Centroid at 0x00 / 0 degrees
  Algorithmic Clock modulo-240 alignment
  Klein (60,15) cross-term lock
  five .o local executable files

NOT REQUIRED:
  SHA-256 loops, MD5 loops, distributed ledger state,
  central page-table lookup, network handshake as core law,
  metadata headers as core law

TYPE: Resolution enforcement
INPUT: Remote resolution requirements
OUTPUT: Remote .imo and local .omi compile to the same .o when inputs match
CONSTRAINTS:
  - Outside carriers may add transport security but do not redefine core law
  - The core does not require cryptographic policy layers
PROTOCOL:
  1. Verify remote resolution depends only on coordinate coherence
  2. Verify no cryptographic loops are required
  3. Verify outside carriers do not redefine core law
ANTI_PATTERN: Requiring SHA-256 or MD5 loops for remote resolution
```

---

## SKILL_21: ENFORCE_STRATIFIED_BOOLEAN_CONTINUUM

```text
PURPOSE: Maintain the local truth-table boundary at n = 6.

BOUNDARY:
  2^6 rows  == 64 row combinations
  2^(2^6)   == 2^64 possible six-input Boolean functions
  1 uint64  == one complete six-input truth-table output column

INCIDENCE BASIS:
  4320       base string surface
  4320^2     = 18,662,400 cross-axis horizon
  4320^(2^2) = 348,285,173,760,000 meta-matrix horizon

TYPE: Boolean continuum enforcement
INPUT: The local truth-table boundary
OUTPUT: Branchless row extraction is preserved
CONSTRAINTS:
  - Branchless row extraction: OUTPUT_BIT = (table_universe >> (input_combination & 0x3F)) & 0x01
  - The horizons are coordinate counts, not sequential row tests
  - Do not claim universal P = NP from the local fixed-width retrieval law
PROTOCOL:
  1. Verify the boundary is at n = 6
  2. Verify branchless row extraction is used
  3. Verify no universal P = NP claims are made
ANTI_PATTERN: Claiming universal P = NP from the local fixed-width retrieval law
```

---

## SKILL_22: ENFORCE_BOUNDED_COMPLEXITY

```text
PURPOSE: Enforce the bounded complexity boundary within the OMINO core.

BOUNDARY:
  2^6 rows
  2^(2^6) possible six-input Boolean functions
  one uint64 selected truth-table output column
  branchless row extraction

CONSTANT-TIME RETRIEVAL:
  target_row = input_combination_6bit & 0x3F
  OUTPUT_BIT = (table_universe >> target_row) & 0x01

TYPE: Complexity enforcement
INPUT: The bounded complexity boundary
OUTPUT: The core resolves declared fixed-width logic surfaces by spatial
        coordinate extraction
CONSTRAINTS:
  - Do not implement cross-axis horizons as sequential row tests,
    heap tables, dynamic search, or runtime task queues
  - Do not claim universal P = NP from the local fixed-width retrieval law
  - Fixed declared local surface -> O(1) register-coordinate retrieval
  - Arbitrary unbounded variable growth -> outside the local claim
PROTOCOL:
  1. Verify the core uses spatial coordinate extraction
  2. Verify no sequential row tests are used
  3. Verify no universal P = NP claims are made
ANTI_PATTERN: Implementing cross-axis horizons as sequential row tests
```

---

## SKILL_23: ENFORCE_THREE_TIER_OMINO_INCIDENCE

```text
PURPOSE: Enforce the three-tier OMINO incidence geometry across all
         15 canonical A-list faces.

TIERS:
  4320       base string surface        clause and literal-token decomposition
  4320^2     interaction surface        assignment and edge-pair decomposition
  4320^4     meta-matrix horizon        cross-axis consistency and parity

A-LIST FACES:
  RULES        (rules.omi)    . (rules.imo)    -> [4320 | 4320^2 | 4320^4]
  FACTS        (facts.omi)    . (facts.imo)    -> [4320 | 4320^2 | 4320^4]
  CLOSURES     (closures.omi) . (closures.imo) -> [4320 | 4320^2 | 4320^4]
  COMBINATORS  (comb.omi)     . (comb.imo)     -> [4320 | 4320^2 | 4320^4]
  CONS         (cons.omi)     . (cons.imo)     -> [4320 | 4320^2 | 4320^4]

TYPE: Incidence enforcement
INPUT: The 15 A-list faces and their three-tier stacks
OUTPUT: Each face carries all three tiers
CONSTRAINTS:
  - The surfaces are not distributed across memory or iterated as sequential tests
  - The field is the Klein (60,15) resolution form
PROTOCOL:
  1. Verify each A-list face carries all three tiers
  2. Verify the surfaces are not distributed across memory
  3. Verify the Klein (60,15) form is preserved
ANTI_PATTERN: Distributing the three-tier surfaces across memory
```

---

## SKILL_24: ENFORCE_WORD_ATOM_ENVIRONMENTS

```text
PURPOSE: Enforce the word-atom environmental space.

ENVIRONS:
  15 formal files
  -> fixed OMINO Centroid, 0x00 / 0 degrees
  -> autonomous omino word atoms
  -> each word atom hosts 72 Circular Slide Rulers
  -> each word atom hosts 4,320 local incidences
  -> pairwise word interaction spans 4320^2 relational points

MATRIX:
  FACE          .omi declarations       .imo definitions        .o executable
  RULES         spatial law bounds      high-plane attributes   target register substrate
  FACTS         ingest constants        input stream bytes      grounded hardware vectors
  CLOSURES      closure thresholds      output coordinates      finite termination lanes
  COMBINATORS   rotation shift maps     vector torque settings  axis clock drives
  CONS          ordered pair templates  low/high plane maps     unified word axes

TYPE: Environmental enforcement
INPUT: The word-atom environmental space
OUTPUT: The 15 formal files are spatial environs around the fixed OMINO Centroid
CONSTRAINTS:
  - The 15 formal files are spatial environs around the fixed OMINO Centroid
  - Each word inside that environment is an autonomous atom
  - The environs do not describe an ordinary software application or storage system
PROTOCOL:
  1. Verify the 15 formal files exist
  2. Verify each word is an autonomous atom
  3. Verify the environs are not described as an ordinary application
ANTI_PATTERN: Describing the environs as an ordinary software application
```

---

## SKILL_25: ENFORCE_OMINO_UNIFIED_NAME_LOCK

```text
PURPOSE: Enforce the OMINO unified-name lock.

NAME: OMINO

INTERPRETATIONS:
  Concentric Slide Rule interpretation
  3D Knowledge Cube interpretation

CENTROID:
  OMINO Centroid
  pinch point: 0x00
  azimuth: 0 degrees

EXTENDED HAMMING: Bit 7 as the complete-cell balance hinge

REQUIRED CODE NAMES:
  Omino, OminoCentroid, OMINO_CENTROID_POINT, OMINO_CENTROID_AZIMUTH,
  OMINO_OVERALL_PARITY, OMINO_BALANCE_HINGE,
  omino_centroid_*, omino_parity_*, omino_balance_*,
  omino_resolve_relative_*, omino_resolve_antipode_*

TYPE: Name lock enforcement
INPUT: The OMINO unified-name lock
OUTPUT: There is one centroid, and antipodal orientation is relative to it
CONSTRAINTS:
  - There is one centroid
  - Antipodal orientation is relative to that centroid
  - The two interpretations are not two objects, identities, authorities,
    or centroids
  - Migration must remain semantic
PROTOCOL:
  1. Verify the OMINO unified-name lock is preserved
  2. Verify there is one centroid
  3. Verify antipodal orientation is relative to the same centroid
ANTI_PATTERN: Introducing a second centroid or identity
```

---

## SKILL_26: ENFORCE_DETERMINISTIC_REMOTE_RESOLUTION

```text
PURPOSE: Ensure deterministic remote resolution across distributed nodes.

SHARED STATE:
  Two nodes resolve the same local coordinate when they share:
  [LOGOS NOMOS FS PATHOS GS RS US OMINO]
  OMINO Centroid at 0x00 / 0 degrees
  Algorithmic Clock modulo-240 alignment
  Klein (60,15) cross-term lock
  five .o local executable files

NOT REQUIRED:
  SHA-256 loops, MD5 loops, distributed ledger state,
  central page-table lookup, network handshake as core law,
  metadata headers as core law

TYPE: Determinism enforcement
INPUT: Remote resolution requirements
OUTPUT: The same declared input and starting coordinate produces the same output
CONSTRAINTS:
  - No wall-clock time, randomness, probability, heuristics, machine learning,
    user identity, network state, filesystem state, environment variables,
    database state, thread scheduling, external policy, or human interpretation
PROTOCOL:
  1. Verify remote resolution is deterministic
  2. Verify no external factors influence resolution
  3. Verify the same input produces the same output
ANTI_PATTERN: Using wall-clock time, randomness, or external state in resolution
```

---

## SKILL_27: ENFORCE_HIGH_DIMENSIONAL_IO

```text
PURPOSE: Enforce the high-dimensional I/O lattice overlay.

ANALOGY:
  stdin  -> 24-vector Leech Lattice Lambda_24 coordinate field
           -> 24 parallel Circular Slide Rulers
  stdout/stderr -> 26-dimensional Lorentzian lattice II_25_1
           -> 0xFFFFFFFF 0xFFFFFFFF null-horizon boundary

CORE UPDATE:
  left_torque  = ROL64(active_axis_state, 1)
  right_torque = ROR64(active_axis_state, 2)
  blended      = left_torque ^ right_torque
  output       = blended ^ 0x1D1D1D1D1D1D1D1DULL

TYPE: I/O enforcement
INPUT: High-dimensional I/O lattice overlay
OUTPUT: The core update is branchless relative to the fixed OMINO Centroid
CONSTRAINTS:
  - I/O is a carrier boundary over declared coordinates
  - Do not treat as permission to replace adapter safety, diagnostics,
    buffering, or tests
PROTOCOL:
  1. Verify the core update is branchless
  2. Verify adapter safety, diagnostics, buffering, and tests are preserved
ANTI_PATTERN: Replacing adapter safety with the I/O lattice overlay
```

---

## SKILL_28: ENFORCE_LATTICE_RETRIEVAL

```text
PURPOSE: Enforce the lattice retrieval boundary.

LEECH LATTICE: Lambda_24
  24-ray boundary represented by 8 quadrants * 3 axes

LORENTZIAN HORIZON: II_25_1
  User-Remote 0xFFFFFFFF 0xFFFFFFFF high-plane boundary

PARABOLIC PATH METRIC:
  Q60(x,y) = 4(4x^2 + 11x^2 + 4xy + y^2)
           = 4(11x^2 + (2x + y)^2)

TYPE: Retrieval enforcement
INPUT: The lattice retrieval boundary
OUTPUT: The retrieval boundary is a fixed coordinate surface
CONSTRAINTS:
  - Do not implement nearest-neighbor scans, lattice search loops,
    thread farms, or dynamic array allocation inside the core
PROTOCOL:
  1. Verify the retrieval boundary is a fixed coordinate surface
  2. Verify no search loops or thread farms are used
ANTI_PATTERN: Implementing nearest-neighbor scans or lattice search loops
```

---

## SKILL_29: ENFORCE_OMINO_CHARACTER_MATRIX_TENSOR

```text
PURPOSE: Enforce the OMINO character matrix tensor organization.

ORGANIZATION:
  LOW PLANE    0x(*/2)0 .. 0x(*/2 + 1)F   32-character tensor window
               terminal control core anchors: 0x(*/2 + 1)C .. 0x(*/2 + 1)F
  HIGH PLANE   low_coordinate ^ 0x80       projected mirror window
               terminates at 0xFF full closure

TYPE: Tensor enforcement
INPUT: The OMINO character matrix tensor
OUTPUT: The tensor window is a coordinate view of the same canonical OMINO
CONSTRAINTS:
  - The low plane is bounded to 0x00..0x7F
  - The high plane is the exact XOR reflection
PROTOCOL:
  1. Verify the low plane is bounded to 0x00..0x7F
  2. Verify the high plane is the exact XOR reflection
ANTI_PATTERN: Treating the high plane as a separate identity
```

---

## SKILL_30: ENFORCE_FIVE_TIER_INTERPRETER_HINGE

```text
PURPOSE: Align the four systematic message-scope bits with the
         negative scope tiers.

HINGE:
  +4 NETWORK  -> Omi-Port Matrix       -> User-Remote Meta-Memory Network
  +3 USER     -> Blackboard Surface    -> User-Public coordinate surface
  +2 SYSTEM   -> Emergent Axial Lisp   -> User-Local System-Land (CAR . CDR)
  +1 KERNEL   -> Hidden .o binaries    -> User-Private register substrate
  +0 SYMBOLS  -> OMINO raw characters  -> OMINO Centroid origin

  -1 US -> bit 6 -> Unit Scope    -> CLOSURES
  -2 RS -> bit 5 -> Record Scope  -> COMBINATORS
  -3 GS -> bit 4 -> Group Scope   -> RULES
  -4 FS -> bit 2 -> File Scope    -> FACTS

SECDED EXTRACTION:
  d1_fs = (inbound_stream_byte >> 2) & 1
  d2_gs = (inbound_stream_byte >> 4) & 1
  d3_rs = (inbound_stream_byte >> 5) & 1
  d4_us = (inbound_stream_byte >> 6) & 1

TYPE: Interpreter hinge enforcement
INPUT: The five-tier interpreter hinge
OUTPUT: The four message-scope bits align with the negative scope tiers
CONSTRAINTS:
  - The SECDED extraction is branchless
  - The five tier interpreter hinge is preserved
PROTOCOL:
  1. Verify the message-scope bits align with the negative scope tiers
  2. Verify the SECDED extraction is branchless
ANTI_PATTERN: Using sequential extraction instead of branchless extraction
```

---

## SKILL_31: ENFORCE_HAMMING_FILE_DISCIPLINE

```text
PURPOSE: Enforce the Hamming file discipline for axial-lisp files.

BITS:
  Bit_1 == FS == FACTS.omi
  Bit_2 == GS == RULES.omi
  Bit_3 == RS == COMBINATORS.omi
  Bit_4 == US == CLOSURES.omi

TRI-LAYER:
  *.omi  == CONS(RULES . COMBINATORS)
  *.imo  == CONS(FACTS . CLOSURES)
  *.o    == CONDENSE(*.omi . *.imo)

TYPE: File discipline enforcement
INPUT: The Hamming file discipline
OUTPUT: The tri-layer file surface is preserved
CONSTRAINTS:
  - Do not replace with JSON, JSONL, XML, tracking databases,
    heap object trees, or ordinary compiler work queues
PROTOCOL:
  1. Verify the Hamming bit assignments are correct
  2. Verify the tri-layer file surface is preserved
  3. Verify no JSON, JSONL, or XML replacements are used
ANTI_PATTERN: Replacing the tri-layer file surface with JSON or XML
```

---

## SKILL_32: ENFORCE_MASTER_MATRIX_HIDDEN_TARGETS

```text
PURPOSE: Enforce the hidden target mapping for the 17-Tier Master Matrix.

HIDDEN TARGETS:
  -8 MODULES        -> ./.omi/modules.o
  -7 CLAUSES        -> ./.omi/clauses.omi and ./.omi/clauses.imo
  -6 CONFIGURATIONS -> ./.omi/configurations.o
  -5 CONS           -> ./.omi/cons.o
  -4 COMBINATORS    -> ./.omi/combinators.o
  -3 CLOSURES       -> ./.omi/closures.o
  -2 FACTS          -> ./.omi/facts.o
  -1 RULES          -> ./.omi/rules.o
   0 SYMBOLS        -> OMINO Centroid origin
  +1..+8            -> upstream semantic matrix

TYPE: Hidden target enforcement
INPUT: The 17-Tier Master Matrix and its hidden targets
OUTPUT: All hidden targets exist and are correctly mapped
CONSTRAINTS:
  - Visible Markdown may carry YAML front matter linking to hidden targets
  - Hidden .o/.omi/.imo tree is under ./.omi/
  - No ad hoc compiler loops, text-parser pipelines, hidden mutable state,
    or cross-repository dependency grafts
PROTOCOL:
  1. Verify all hidden targets exist under ./.omi/
  2. Verify the mapping is correct
  3. Verify no cross-repository dependency leakage
ANTI_PATTERN: Creating ad hoc compiler loops or text-parser pipelines
```

---

## SKILL_33: ENFORCE_OMINO_CENTROID_PHASE_LOCK

```text
PURPOSE: Enforce the OMINO Centroid Phase Position Lock.

CENTROID:
  OMINO Centroid
  pinch point: 0x00
  azimuth: 0 degrees

TYPE: Phase lock enforcement
INPUT: The OMINO Centroid Phase Position Lock
OUTPUT: All coordinate derivations reference the fixed OMINO Centroid
CONSTRAINTS:
  - All rotations are relative to the fixed centroid
  - Antipodal orientation is relative to the same centroid
  - No second origin is introduced
PROTOCOL:
  1. Verify all coordinate derivations reference the OMINO Centroid
  2. Verify no second origin is introduced
ANTI_PATTERN: Introducing a second origin or using absolute coordinates
```

---

## SKILL_34: ENFORCE_SYSTEMATIC_HAMMING_CODE_CELL

```text
PURPOSE: Enforce the Systematic Hamming Code Cell Integration.

CELL: The [8,4,4] SECDED streaming cell structure

TYPE: Hamming cell enforcement
INPUT: The systematic Hamming code cell
OUTPUT: The cell structure is preserved
CONSTRAINTS:
  - The bit map is correct
  - The outcome matrix is preserved
  - The bit-7 hinge and OMINO centroid are one canonical OMINO
PROTOCOL:
  1. Verify the cell structure is correct
  2. Verify the outcome matrix is preserved
ANTI_PATTERN: Treating the bit-7 hinge and OMINO centroid as two names
```

---

## SKILL_35: ENFORCE_TRI_LAYER_AXIAL_LISP_FILE_ARCHITECTURE

```text
PURPOSE: Enforce the tri-layer axial-lisp file architecture.

LAYERS:
  *.omi    User-Local Resolution
  *.o      Local-Binary Resolution
  *.imo    User-Remote Resolution

TYPE: File architecture enforcement
INPUT: The tri-layer axial-lisp file architecture
OUTPUT: The three layers remain distinct and non-leaking
CONSTRAINTS:
  - Do not replace with linear parser staging, mutable heap records,
    or sequential memory access
PROTOCOL:
  1. Verify the three layers exist and are distinct
  2. Verify no linear parser staging is used
ANTI_PATTERN: Replacing the tri-layer pattern with linear parser staging
```

---

## SKILL_36: ENFORCE_BRANCHLESS_ALGEBRAIC_STATE_TRANSFORMATION

```text
PURPOSE: Enforce branchless algebraic state transformation.

TRANSFORMATION:
  current coordinate -> XOR/shift/rotate transform -> next coordinate

TYPE: Transformation enforcement
INPUT: Algebraic state transformations
OUTPUT: All transformations are branchless relative to the fixed OMINO Centroid
CONSTRAINTS:
  - Semantic jumps are prohibited in core arithmetic kernels
  - Bounded control flow is permitted for outside-in parsing, bounds checks,
    conformance tests, diagnostics, and hardware adapters
PROTOCOL:
  1. Verify all core transformations are branchless
  2. Verify semantic jumps are prohibited in core kernels
  3. Verify bounded control flow is permitted for outside-in tasks
ANTI_PATTERN: Using semantic jumps in core arithmetic kernels
```

---

## SKILL_37: ENFORCE_OMNICRON_OBJECT_MODEL_COMPILATION_TARGET

```text
PURPOSE: Enforce the omnicron-object-model compilation target.

TARGET: The omnicron-object-model is the master four-plane circular runtime resolver

TYPE: Compilation target enforcement
INPUT: The omnicron-object-model compilation target
OUTPUT: The compilation target is preserved
CONSTRAINTS:
  - The omnicron-object-model is the master four-plane circular runtime resolver
  - The omino-tensor-model is the 16-bit word-axis substrate
PROTOCOL:
  1. Verify the compilation target is correct
  2. Verify the omino-tensor-model is the substrate
ANTI_PATTERN: Treating the omnicron-object-model as something other than
              the master four-plane circular runtime resolver
```

---

## SKILL_38: ENFORCE_GAUGE_INCIDENCE_WITNESS_INVARIANT_PHASE_LOCK

```text
PURPOSE: Enforce the gauge-incidence witness invariant phase lock.

WITNESS: 0x18 == 24 decimal == 4!

TYPE: Phase lock enforcement
INPUT: The gauge-incidence witness invariant
OUTPUT: Every sector reduces to 0x18
CONSTRAINTS:
  - The 0x18 witness belongs to Metatron Incidence Scribe
  - High sector prefix cancels under XOR
PROTOCOL:
  1. Verify all four sectors reduce to 0x18
  2. Verify the high sector prefix cancels under XOR
ANTI_PATTERN: Calling Metatron a parser or generic resolver
```

---

## SKILL_39: ENFORCE_METATRON_INCIDENCE_SCRIBE_HARDWARE_LOCK

```text
PURPOSE: Enforce the Metatron Incidence Scribe hardware lock.

OWNERSHIP:
  0x18
  0x0001, 0x0010, 0x0100, 0x1000, 0x10000

HEXAGONAL STEPPING:
  0x0001   -> US incidence, bit 0 slot
  0x0010   -> RS incidence, bit 4 slot
  0x0100   -> GS incidence, bit 8 slot
  0x1000   -> FS incidence, bit 12 slot
  0x10000  -> gauge carry, bit 16 slot

TYPE: Hardware lock enforcement
INPUT: The Metatron Incidence Scribe ownership
OUTPUT: Metatron owns the gauge-incidence witness
CONSTRAINTS:
  - Agents must not call Metatron a parser, policy layer, generic resolver,
    or balanced-word controller
PROTOCOL:
  1. Verify Metatron ownership is correct
  2. Verify Metatron is not called a parser or generic resolver
ANTI_PATTERN: Calling Metatron a parser or generic resolver
```

---

## SKILL_40: ENFORCE_DOUBLE_EXPONENTIAL_TRUTH_TABLE_META_MATRICES

```text
PURPOSE: Enforce the double-exponential truth-table meta-matrices.

BOUNDARY:
  2^6 rows == 64 row combinations
  2^(2^6) == 2^64 possible six-input Boolean functions
  1 uint64 == one complete selected six-input truth-table output column

TYPE: Meta-matrix enforcement
INPUT: The double-exponential truth-table meta-matrices
OUTPUT: The boundary is preserved at n = 6
CONSTRAINTS:
  - The horizons are coordinate counts, not sequential row tests
  - Do not claim universal P = NP from the local fixed-width retrieval law
PROTOCOL:
  1. Verify the boundary is at n = 6
  2. Verify no universal P = NP claims are made
ANTI_PATTERN: Claiming universal P = NP from the local fixed-width retrieval law
```

---

## SKILL_41: ENFORCE_FIVE_FILE_LOCAL_FRAME_RESOLUTION_LOCK

```text
PURPOSE: Enforce the five-file local frame resolution lock.

SUBSTRATE: rules.o, facts.o, closures.o, combinators.o, cons.o

TYPE: Resolution lock enforcement
INPUT: The five-file local frame resolution lock
OUTPUT: The .o layer is the direct local register substrate
CONSTRAINTS:
  - The .o layer is the direct local register substrate
  - .omi/.imo surfaces remain declaration and definition views
PROTOCOL:
  1. Verify the .o layer is the direct local register substrate
  2. Verify .omi/.imo surfaces remain declaration and definition views
ANTI_PATTERN: Treating .o files as application binaries
```

---

## SKILL_42: ENFORCE_FIVE_FILE_THREE_SURFACE_DIRECT_REGISTER_BINDING

```text
PURPOSE: Enforce the five-file three-surface direct register binding.

BINDING:
  rules.omi / rules.imo / rules.o
  facts.omi / facts.imo / facts.o
  closures.omi / closures.imo / closures.o
  combinators.omi / combinators.imo / combinators.o
  cons.omi / cons.imo / cons.o

TYPE: Register binding enforcement
INPUT: The five-file three-surface direct register binding
OUTPUT: Each file carries the three surfaces
CONSTRAINTS:
  - Each file carries the three surfaces
  - The three surfaces remain distinct and non-leaking
PROTOCOL:
  1. Verify each file carries the three surfaces
  2. Verify the three surfaces remain distinct
ANTI_PATTERN: Merging the three surfaces into one
```

---

## SKILL_43: ENFORCE_LEECH_LATTICE_AND_II_25_1_LORENTZIAN_RETRIEVAL_LOCK

```text
PURPOSE: Enforce the Leech Lattice and II_25_1 Lorentzian retrieval lock.

LEECH LATTICE: Lambda_24
  24-ray boundary represented by 8 quadrants * 3 axes

LORENTZIAN HORIZON: II_25_1
  User-Remote 0xFFFFFFFF 0xFFFFFFFF high-plane boundary

TYPE: Retrieval lock enforcement
INPUT: The Leech Lattice and II_25_1 Lorentzian retrieval lock
OUTPUT: The retrieval boundary is a fixed coordinate surface
CONSTRAINTS:
  - Do not implement nearest-neighbor scans, lattice search loops,
    thread farms, or dynamic array allocation inside the core
PROTOCOL:
  1. Verify the retrieval boundary is a fixed coordinate surface
  2. Verify no search loops or thread farms are used
ANTI_PATTERN: Implementing nearest-neighbor scans or lattice search loops
```

---

## SKILL_44: ENFORCE_SYSTEMATIC_OMINO_CHARACTER_MATRIX_TENSOR

```text
PURPOSE: Enforce the systematic OMINO character matrix tensor.

ORGANIZATION:
  LOW PLANE    0x(*/2)0 .. 0x(*/2 + 1)F   32-character tensor window
  HIGH PLANE   low_coordinate ^ 0x80       projected mirror window

TYPE: Tensor enforcement
INPUT: The systematic OMINO character matrix tensor
OUTPUT: The tensor window is a coordinate view of the same canonical OMINO
CONSTRAINTS:
  - The low plane is bounded to 0x00..0x7F
  - The high plane is the exact XOR reflection
PROTOCOL:
  1. Verify the low plane is bounded to 0x00..0x7F
  2. Verify the high plane is the exact XOR reflection
ANTI_PATTERN: Treating the high plane as a separate identity
```

---

## SKILL_45: ENFORCE_COBS_CONS_16X16_BLACKBOARD_BOUNDING_MATRIX

```text
PURPOSE: Enforce the COBS CONS 16x16 Blackboard bounding matrix.

BOUNDING RECTANGLES:
  Local:
    (0x00, 0x07, 0x37, 0x30)
    (0x08, 0x0F, 0x3F, 0x38)
    (0x40, 0x47, 0x77, 0x70)
    (0x48, 0x4F, 0x7F, 0x78)
  Remote:
    (0x80, 0x87, 0xB7, 0xB0)
    (0x88, 0x8F, 0xBF, 0xB8)
    (0xC0, 0xC7, 0xF7, 0xF0)
    (0xC8, 0xCF, 0xFF, 0xF8)

TARGET REDUCTION: CONS = 0xFFFFFFFF

TYPE: Bounding matrix enforcement
INPUT: The COBS CONS 16x16 Blackboard bounding matrix
OUTPUT: The bounding rectangles are correct and the target reduction is 0xFFFFFFFF
CONSTRAINTS:
  - No row or column traversal as nested loops inside the core
PROTOCOL:
  1. Verify the bounding rectangles are correct
  2. Verify no nested loops are used for traversal
ANTI_PATTERN: Using nested loops for row or column traversal
```
